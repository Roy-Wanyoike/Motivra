# Backend — Platform Foundation

Shared Go primitives for every domain service: configuration, middleware, database (pgx) access, OpenTelemetry wiring, NATS event publishing helpers and graceful shutdown. The platform-foundation engineer owns this zone and its service template.

Owned by the platform-foundation owner; see docs/ARCHITECTURE.md §2 — do not edit outside your zone.
