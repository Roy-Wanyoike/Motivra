# Motivra Drive environment variables (copy to .env.local)
# No secrets in NEXT_PUBLIC_* — these are inlined into the client bundle.

# Base URL of the Motivra API gateway. Empty = same-origin, which matches the
# `servers: url: /` declaration in contracts/*/openapi.yaml.
NEXT_PUBLIC_API_BASE_URL=
